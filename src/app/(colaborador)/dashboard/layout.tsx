// src/app/(colaborador)/dashboard/layout.tsx
import type { ReactNode } from 'react';

export default function DashboardPassthroughLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
